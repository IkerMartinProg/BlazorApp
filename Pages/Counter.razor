@page "/counter"

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@*
    @currentCount --> variable que sirve como contador
    @onclick="IncrementCount" --> ejecuta el método "IncrementCount"
*@

@code {
    private int currentCount = 0;

    [Parameter]
    public int IncrementAmount { get; set; } = 1;

    private void IncrementCount()
    {
        currentCount += IncrementAmount;
    }
}

@*
    Variables declaradas:
        -currentCount (valor inicial)
        -IncrementAmount (cantidad del incremento)
    
    Método:
        -IncrementCount: aumenta el contador en 1.
        currentCount = currentCount + IncrementAmount;
        currentCount += IncrementAmount;
*@
